<% if (page.title) { %>
    <% if (page.layout==='sims')  { %>
        <main>
    <%} else {%> 
        <% if (page.layout==='description')  { %>
            <main>
        <%} else {%> 
            <div id="between-head-and-foot">
            <main>
            <h1 id="page-title"><%- page.title %></h1>
        <%} %>
    <%} %>
<% } %>
<%- page.content %>
</div>